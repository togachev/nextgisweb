export function imoduleSelectedFeature(display) {

    const featuresInfo = [];

    // display.imodule.fetch({
    //     queryOptions: { deep: true },
    //     query: { type: "layer" },
    //     onItem: (item) => {
    //         console.log(item);
    //     },
    // });
    
    console.log(display.imodule);
    
    return featuresInfo;
}
